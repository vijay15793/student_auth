<% if @user && @user.errors.size > 0 %>
    <div align="center">
         <h6><%= pluralize(@user.errors.size, "error") %> 
        prohibited this record from being saved</h6>
        <p>There were problems with the following fields:</p>
        <% @user.errors.full_messages.each do |msg| %>
            <%= msg %><br />
        <% end %>
         </div>
     <% end %>